# Interface acceptance contract ledger

Evidence refresh: 2026-09-05. The current platform integration base is
`8930f9fbef2bd3bb2889450bf0dfb46c0edbcd4e` (canonical Library projection
cutover). Recipe authority is `10db3c7a73a18319f81103448ddaeada50334bb2`,
tag `v1.0.1`. This ledger separates implementation, local verification, CI,
publication/deployment, and physical hardware evidence. The companion
[launch progress ledger](interface-progress-2026-09-05.md) records the same
boundaries and current release/deployment identities.

## Current integrated evidence snapshot

The platform tree includes the canonical profile, NAS, runtime and Library
integration merges through `8930f9fb`. Commits `4bd63920` and `a25b5d38`
verify durable progress projection and cache/progress integration; `1f6e1362`
keeps async distribution hashing on a heap buffer for the normal Tokio stack.
The protocol wheel evidence below is historical and remains separate from the
current integration head. Its recorded SHA-256 is
1eaba528efc74564b6dc00a6dada42a59e12e50fb52d37e6c966da263a670448.

The integrated runtime evidence remains historical evidence, not a current
head result. The previous full Controller evidence collected 2,039 cases:
1,935 passed, 103 skipped, and one macOS-only collection limitation because
os.setresgid is unavailable during collection on macOS. The
equivalent privilege-drop contract passed in read-only-mounted Linux under
Docker context orbstack (OrbStack, aarch64). These are repository and
container boundaries, not deployed Controller or hardware acceptance.

The Rust telemetry test evidence is exactly 25 tests passed at commit
`1aeeb310`. That is test execution evidence for the telemetry producer and
does not represent a metric-count requirement, deployed observation, or
physical sensor acceptance.

The P9 owner reports the current integration head `8930f9fb` connected result
as 4 OrbStack checks passed and 3 PostgreSQL checks passed, with evidence
summary `5168777d`. This is connected local acceptance evidence; it is not CI,
Controller publication/deployment, or physical Spark acceptance.

Durable progress evidence is
control/tests/test_distribution_executor.py::test_production_composite_uncached_cache_then_two_target_distribution,
including nonzero parent/member byte counters, restart projection, and
unknown-total handling. The local production composition slice is six cases;
the 49-case worker branch remains separate evidence. The normal macOS Rust
command cargo test --locked -p vonk-agent --lib cannot collect because
rustix::fs::openat2 is Linux-gated; the normal-stack fix itself is 1f6e1362.
A Linux CI/ARM64 result is required before calling the Rust path verified.

The current recipe release supplies a schema-2 catalog index with 92 Model
documents and 84 Recipe documents. Seventy-nine Model identities are directly
referenced by a Recipe; 13 have no direct Recipe relationship. This is catalog
and package closure evidence, not runtime producer or physical support proof.
The index is the `v1.0.1` release at recipe commit `10db3c7a`; this ledger does
not infer a new platform publication or deployment from the package count.

The recorded recipe package consumer evidence includes 88 passing fixtures
covering changed-only, restart, failure, offline and fork behavior. This is
local fixture evidence; it does not prove publication, deployed Controller or
physical catalog acceptance.

Web evidence is separately recorded as merge
`7eb783d63c5ea87d2efea8834467ddcda52decfd`, production workflow
`33985962484`, and deployment
`https://abd3b57b.vonk-forge-web.pages.dev`. This proves the web publication
boundary only. Root visual review, connected Controller/API and Run/Switch,
and physical Spark gates remain unresolved here.

Status vocabulary:

- **Bound** means the production service, route, and named focused test exist.
- **Partial** means a binding exists but one or more required consumers, states,
  or journeys remain incomplete.
- **Unresolved** means the observable acceptance proof is still missing.

## Endpoint and operation registry

These are the operation names present in the integrated Controller source. The
generic Activity routes are the cross-family read surface; family routes remain
authoritative for family-specific mutation and evidence.

| Surface | Status | Actual routes and operation IDs | Remaining contract boundary |
|---|---|---|---|
| Global Activity | Bound | `GET /api/v1/operations` (`listOperations`); `GET /api/v1/operations/{operation_id}` (`getOperation`) | Read-only global projection. Recovery actions are shown only when a family advertises a real mutation route. |
| NAS model cache | Bound | `GET /api/v1/model-cache` (`getModelCacheInventory`); `GET /api/v1/model-cache/entries/{artifact_set_sha256}` (`getModelCacheEntry`); `POST /api/v1/model-cache/download-preview` (`previewModelCacheDownload`); `POST /api/v1/model-cache/download` (`downloadModelCache`); repair, eviction, updates, and family operation list/detail use `previewModelCacheRepair`, `repairModelCache`, `previewModelCacheEviction`, `evictModelCache`, `getModelCacheUpdates`, `listModelCacheOperations`, `getModelCacheOperation` | Family operation documents and global Activity documents must retain the same digest, progress, and evidence identity. |
| Saved profiles | Bound | `listFleetProfiles`, `createFleetProfile`, `captureCurrentFleetProfile`, `getFleetProfile`, `updateFleetProfile`, `deleteFleetProfile`, `previewFleetProfile`, `getFleetProfileStatus`, `duplicateFleetProfile`, `applyFleetProfile`, `previewFleetProfilePreparation`, `prepareFleetProfile`, `switchFleetProfile`, `getFleetProfileApplication` | Full browser journey and deployed whole-fleet switch evidence remain open. |
| Recipe/run operations | Bound | `previewRecipeRun`, `startRecipeRun`, `getRecipeRunStatus`, `getRecipeOperation`, `retryRecipeOperation`, `stopRecipeRun`; build and image distribution use `previewRecipeBuild`, `buildRecipe`, `previewRecipeImageDistribution`, `distributeRecipeImage` | Family route is authoritative for mutations; global Activity is the inspection union. |
| Telemetry | Partial | `getNodeTelemetryCurrent`, `getNodeTelemetryCapabilities`, `listNodeTelemetryWorkloads`, `getNodeTelemetryHistory` | Native sensor availability, full metric-family coverage, and browser chart/export journeys remain open. |
| CLI Activity | Partial | `operations list/show/watch/wait/evidence` calls the generic routes; cache operation list/show still call `/api/v1/model-cache/operations` | Cross-client normalized end-to-end proof is not complete. |

## A01–A23 acceptance ledger

| ID | Status and implemented production binding | Focused test path | Fixture-only evidence | CI, deployment, and hardware boundary | Unresolved gap |
|---|---|---|---|---|---|
| A01 | **Bound.** `ModelCacheService` owns exact artifact manifests and queued download/verify work through `previewModelCacheDownload` and `downloadModelCache`. | `control/tests/test_model_cache.py::test_download_persists_real_primary_and_auxiliary_bytes_and_deduplicates`; `control/tests/test_model_cache.py::test_download_mutation_is_queued_until_the_controller_worker_runs`; `control/tests/test_model_cache.py::test_activity_provider_filters_pages_and_projects_attempt_and_progress` | Tiny local artifacts are downloaded, verified, deduplicated, and projected into Activity. | No live upstream source, NAS deployment, or zero-Spark environment was exercised; no publication claim. | Run the actual Controller/NAS cache journey with all Sparks offline and verify web/CLI cache state. |
| A02 | **Partial.** distribution.py and distribution_executor.py bind one verified model/image source to multiple target operations; run/switch consumes the source. Durable nonzero member progress was verified in `4bd63920` and is carried into the current local integration. | `control/tests/test_model_cache.py::test_one_set_with_shared_digest_counts_one_physical_payload`; `control/tests/test_run_switch_operations.py::test_child_distribution_progress_is_typed_and_restart_safe`; `control/tests/test_distribution_executor.py::test_production_composite_uncached_cache_then_two_target_distribution` | Shared digest accounting, per-target verification, nonzero member counters, restart projection, and unknown totals are fixture-backed. | Two-target tests use disposable services; no two physical Sparks, LAN transfer, or measured WAN request count. | Execute a two-Spark delivery and prove one upstream payload plus verified destination identities. |
| A03 | **Bound.** Cache and child distribution operations persist checkpoints and resume through worker restart. | `control/tests/test_model_cache.py::test_interrupted_download_checkpoint_resumes_after_service_restart`; `control/tests/test_run_switch_operations.py::test_artifact_child_checkpoint_and_digest_mismatch_fail_closed` | Restarted local services resume valid bytes and reject changed digests. | No container restart in a deployed Controller or hardware transfer interruption was run. | Add a deployed/CI restart journey proving no duplicate publication after interruption. |
| A04 | **Bound.** Same-pin repair quarantines and verifies a replacement before atomic swap; old verified data remains usable on failure. | `control/tests/test_model_cache.py::test_same_pin_repair_verifies_before_atomic_replace_and_preserves_old_bytes`; `control/tests/test_model_cache.py::test_verified_serving_seam_refuses_incomplete_or_tampered_sets` | Local filesystem fixtures prove old-copy preservation and tamper rejection. | No NAS storage fault or deployed repair was exercised. | Verify web/CLI repair failure and pinned-profile behavior against a deployed cache. |
| A05 | **Bound.** Durable references protect cache content; eviction is explicit and reports protected/reclaimable bytes. | `control/tests/test_model_cache.py::test_protection_is_derived_from_durable_references_and_blocks_eviction`; `control/tests/test_recipe_operations.py::test_uninstall_keeps_model_when_another_installed_recipe_uses_it` | SQLite/local cache fixtures cover shared references and blocked eviction. | No live stop/uninstall or NAS volume retention check. | Prove shared cache survives deployment lifecycle and that actual reclaimable bytes match storage. |
| A06 | **Partial.** Catalog Model and Recipe/runtime contracts are separate typed authorities; the `v1.0.1` recipe release contains 92 Models and 84 Recipes, with 79 direct Model references and 13 Models without a direct Recipe. | `control/tests/test_catalog_api.py::test_public_recipe_qualification_requires_explicit_unambiguous_acceptance`; capability contract tests in `control/tests/test_builtin_harnesses.py`; capability projections in `control/tests/test_library_projection.py` | Fixture catalog rows distinguish declared and unknown support; package/index closure is descriptive inventory evidence only. | No runtime producer/adapter proves every Model capability, and no full web/CLI comparison journey or physical serving proof is claimed. | Add authoritative model capability producers/adapters and a real vision-advertised/text-only comparison with stable unknown reasons across both clients. |
| A07 | **Bound.** Fleet profile scope, run/switch planning, stop/start ordering, retention, and child progress are wired through production services. | `control/tests/test_fleet_profiles.py::test_profile_apply_switches_dual_solo_idle_and_reuses_cached_installation`; `control/tests/test_run_switch_operations.py::test_resource_constrained_switch_exposes_after_stop_fit_and_orders_stop_before_prepare` | Disposable profile/run fixtures cover distributed, solo, idle, and reuse cases. | No deployed profile switch or physical two-Spark transition. | Run A→B→A through the actual Controller and record retained/reused artifacts and per-Spark results. |
| A08 | **Bound.** Explicit profile scope and idle assignments are persisted; unlisted scope members are reconciled by the profile planner. | `control/tests/test_fleet_profiles.py::test_all_idle_profile_has_explicit_scope_and_no_preparation`; `control/tests/test_fleet_profiles.py::test_profile_scope_reconciles_idle_member_and_retains_reusable_installation` | Fixture profiles prove all-idle and scoped idle outcomes. | No browser save/apply journey or live fleet state. | Prove empty desired run sets and excluded Sparks through web and CLI against a deployed Controller. |
| A09 | **Partial.** Profile CRUD, capture, duplicate, assignments, scope validation, and exact pins are production routes. | `control/tests/test_fleet_profile_api.py::test_profile_contract_exposes_capture_duplicate_status_prepare_and_switch`; `control/tests/test_fleet_profiles.py::test_profile_validation_rejects_unknown_sparks_and_recipe_topology_drift` | Service fixtures cover drafts, duplicate identity, scope, and assignment validation. | Browser journeys for offline editing, duplicate/remove, stale scope, and mobile flow are incomplete or skipped. | Complete the browser and CLI editor journeys and record offline/busy Spark behavior. |
| A10 | **Partial.** Profile and recipe preview/apply routes bind plan digests, stale-plan rejection, and durable applications. | `control/tests/test_fleet_profile_api.py::test_profile_preview_exposes_blockers_and_apply_rejects_stale_plan`; `control/tests/test_fleet_profiles.py::test_profile_apply_rejects_a_stale_preview_and_request_key_reuse` | Fixtures prove exact plans and stale rejection, including long step sets. | No full browser review or deployed stale-apply journey; normal user flow was simplified after the original review. | Verify a greater-than-six-step impact is inspectable and applied exactly through both clients. |
| A11 | **Bound.** Request-key/idempotent durable operations are persisted; unknown responses reconcile by durable ID and timeout does not cancel. | `control/tests/test_jobs.py::test_repeated_request_key_replays_one_durable_job`; `control/tests/test_operation_api.py::test_durable_operation_keyset_pages_are_complete_and_aggregated`; `control/tests/test_recipe_operations.py::test_postgres_duplicate_stop_request_returns_same_operation` | SQLite/PostgreSQL fixtures cover replay, keyset pagination, and concurrent mutation winners. | No lost-response test against a deployed Controller or CLI/web cross-client run. | Execute a real connection-loss/reconcile journey and capture the same operation identity from both clients. |
| A12 | **Partial.** Per-member run/profile state, rank health, recovery deadlines, typed failure evidence, and sanitized projections are implemented. | `control/tests/test_recipe_operations.py::test_partial_multinode_stop_retains_every_active_capacity_reservation`; `::test_distributed_rank_loss_withdraws_route_when_recovery_authority_is_missing`; `control/tests/test_operation_contract.py` recovery/evidence tests | Disposable multi-node fixtures prove partial state, checkpoint/recovery transitions, and redaction. | No physical member failure, deployed evidence collection, or hardware acceptance. | Run a real distributed member-loss journey and verify non-healthy group, remaining workloads, and recovery action. |
| A13 | **Partial.** Web and CLI share Controller routes, generated client models, generic Activity list/detail, and family operation providers. | `control/tests/test_operation_api.py::test_global_operation_projection_merges_typed_provider_families`; `control/tests/test_operation_api.py::test_profile_operation_provider_is_registered_through_the_global_api`; CLI command/route tests in `tests/cluster_profiles/test_controller_cli.py` where present | Fixture provider tests show cache/profile/run-shaped rows merge into one typed list. | No complete normalized-plan/result parity journey in a deployed service; skipped browser coverage remains. | Prove web-created inspectability from CLI and CLI-created inspectability from web for the same operation. |
| A14 | **Partial; local UI branch only.** The compact paired-list Controller-web change is implemented and tested at `5db64a31`; it is not yet integrated into platform head `8930f9fb`. | `control/web/e2e/fleet-library.spec.ts::Library pairs exact model selection with matching recipes on desktop and mobile`; the branch also retains the focused Fleet compact/topology and overflow checks. | The local Playwright fixture proves the paired model/recipe selection behavior and selected responsive states. | No generated-API integration or deployed evidence is claimed for `5db64a31`. The separate public web tuple does not certify this local branch. | Integrate the branch, regenerate API bindings as required, and run the connected browser/deployed visual gate across the required widths. |
| A15 | **Partial.** Keyboard/focus foundations, loading/error/empty components, and reduced-motion styles exist. | `control/web/e2e/fleet-library.spec.ts::Fleet Detailed view and bounded history are keyboard-accessible with local evidence`; `control/web/e2e/fleet-library.spec.ts::Fleet discovery searches friendly names and combines actionable health filters`; `control/web/e2e/fleet-library.spec.ts::Add Spark preserves an in-flight and revealed one-time grant until an explicit decision`; `control/web/e2e/fleet-library.spec.ts::Library retries a failed snapshot and recipe detail request`; `control/web/e2e/fleet-library.spec.ts::Empty Library offers a direct escape to the custom runtime builder`; `control/web/e2e/artifact-job-workspace.spec.ts::exposes explicit empty and failed retry recovery states`; `control/web/e2e/artifact-job-workspace.spec.ts::keeps variant evidence and native input contracts accessible without phone overflow` | Playwright fixture/component checks cover focus and selected states; web-history correction and former-skip disposition remain pending reconciliation. | No complete browser keyboard/mobile journey, Safari/touch run, or measured live announcements; the opt-in screenshot remains skipped unless `IMPECCABLE_REVIEW=1`. | Root visual review and Profile/Metrics direct new browser journeys remain unresolved pending the web report; run the complete connected browser journeys including progress, retry, focus return, and reduced motion. |
| A16 | **Partial.** Backend OpenAPI operation IDs and generated clients are present in the current integration; the protocol wheel/SBOM checks below are historical evidence. | `control/tests/test_operation_api.py::test_admin_operation_schema_declares_applicable_bounded_errors`; `control/tests/security/test_agent_protocol.py::test_release_artifacts_install_the_exact_protocol_wheel`; `control/tests/security/test_agent_protocol.py::test_protocol_has_no_arbitrary_operation_member`; generated-client regeneration from `19b4ba3f` | Typed OpenAPI/client fixtures and the checked-in wheel hash validate source shape. | Local wheel-environment subprocess checks require a materialized uv environment; no current-head CI or deployed-client verification is claimed. | Run the complete generated-client/protocol CI lane and verify clients against deployed backend. |
| A17 | **Partial.** `TelemetryCollector` emits bounded per-device, per-interface, per-process and per-run series for the required host and runtime families. `TelemetryMetrics` schema 2 travels inside the authenticated schema-1 `POST /agent/v1/telemetry` envelope; Controller persistence assigns the authenticated node and receive time, maintains raw and rich rollups, and serves typed current, capability, workload and history projections through `getNodeTelemetryCurrent`, `getNodeTelemetryCapabilities`, `listNodeTelemetryWorkloads` and `getNodeTelemetryHistory`. Generated OpenAPI, CLI route/export bindings, and the web detail current/capability/workload tabs are present. | Rust `rust/crates/vonk-agent/tests/telemetry.rs` collector/runtime fixtures; `control/tests/test_agent_api.py` authenticated/schema checks; `control/tests/test_telemetry.py`, `control/tests/test_telemetry_maintenance.py`, and `control/tests/test_fleet_projection.py` persistence/history/projection checks; `tests/control/test_openapi_clients.py`; `tests/cluster_profiles/test_controller_cli.py`; `control/web/src/api/client.test.ts` | Local fixtures cover bounded payloads, explicit unsupported reasons, provenance, and route filters. A populated rich-metrics Controller ingestion/rollup fixture is not present. | The focused Python tests are local evidence only; the Rust fixture suite is checked in but could not compile on macOS because the pre-existing `rustix` `ResolveFlags`/`openat2` Linux gate is unavailable. No CI run, deployed agent/Controller observation, or Spark hardware result is established by this branch. | Add a populated rich-metrics Controller ingestion/rollup fixture and finish the web rich-history chart/export mapping; leave browser history validation separate from this correction. |
| A18 | **Partial.** The native producer queries `nvidia-smi` for per-GPU utilization, temperature, draw, limit, SM clocks, throttle flags and process memory, `/proc/meminfo` for host memory, and `nvidia-smi dmon` for memory bandwidth. GB10 handling labels one `unified-memory-pool` with `memory.pool=shared-unified`, retains GPU-attributed allocation, and omits dedicated GPU total/free so consumers cannot count the physical pool twice. OOM pressure remains unavailable when only capacity/used-percent data exists, and `gpu.throttle_active` is emitted only when at least one throttle reason is known. | `rust/crates/vonk-agent/tests/telemetry.rs::collects_literal_cpu_memory_disk_network_and_accelerator_metrics`; `::counter_resets_and_missing_sources_remain_unknown`; `::oom_pressure_remains_unavailable_when_only_memory_capacity_is_known`; `::unavailable_throttle_flags_do_not_become_false_active_state`; `control/tests/test_telemetry.py::test_capacity_pairs_are_both_known_or_both_unknown`; generated telemetry model checks in `tests/control/test_openapi_clients.py` | The GB10-like collector fixture proves host capacity and dedicated GPU total/free stay unknown while native utilization, temperature and draw remain available; the exact25 Rust telemetry tests include truthful OOM/throttle availability behavior. | Python evidence is local and fixture-backed; Rust execution is blocked on the macOS-only `rustix` compile error. No physical GB10/Spark sensor accuracy or 128 GiB run is claimed. | Verify the one physical 128 GiB pool and sensor accuracy on hardware; no OOM or throttle semantics remain to be inferred from a used-percent proxy or unavailable flags. |
| A19 | **Partial.** The agent persists sequence state across process restarts, queues at most 15 samples, retries the authenticated report with bounded backoff, and records counter-reset gaps for CPU, disk, network, power and runtime rates. Controller ingestion rejects stale/future, out-of-order and conflicting observations, updates the latest row transactionally, and maintains minute, 15-minute and daily rich rollups with bounded raw/history windows and series identity/provenance. | `rust/crates/vonk-agent/tests/telemetry.rs::counter_resets_and_missing_sources_remain_unknown`; `::sequence_reservation_survives_same_boot_process_restart`; `::queue_keeps_fifteen_samples_and_preserves_exact_gap_on_the_oldest_retained`; `control/tests/test_agent_api.py` stale/order/schema checks; `control/tests/test_telemetry.py` history/replay checks; `control/tests/test_telemetry_maintenance.py::test_late_sample_reruns_minute_and_quarter_hour_rollups` | Fixtures cover reset-to-gap, missing optional sources, queue bounds, replay/order rejection, late samples and retention. Rich rollup identity is preserved through the maintenance path. | The Rust command is blocked by the host-specific macOS `rustix` Linux API gate; no deployed offline interval, collection gap or failing physical sensor was observed. | Rich rollup summaries retain numeric identity, unit, source, measurement kind and aggregation, while support/freshness/reason and per-series observed/received timestamps are not retained at rollup level. The API supports bounded raw/minute/15-minute/daily ranges, but the web toolbar still exposes 30 days rather than the specified 31-day history. Add rich rollup fixtures and deployed offline/failing-sensor evidence. |
| A20 | **Partial.** The agent has managed-local allowlisted adapters: Prometheus `/metrics` for vLLM, SGLang and llama.cpp, with DS4/EXL3 contracts present but explicitly unavailable until truthful adapters emit their own metrics; ComfyUI uses `/queue`. Known keys cover decode/prefill (including cached/uncached when exposed), output tokens, slots/queue, KV/cache/preemptions and histogram-derived TTFT/E2E/ITL p95. Direct mean/current latency samples are not relabeled as p95; counters reset to gaps and absent backend fields become named unsupported capabilities. Controller joins authenticated runtime IDs to RecipeRun/RunNode placement and sanitized ArtifactJob/Job workloads. | `rust/crates/vonk-agent/tests/telemetry.rs::vllm_runtime_adapter_collects_throughput_queue_cache_and_histogram_p95`; `::runtime_latency_does_not_relabel_mean_or_current_as_p95`; `::namespaced_runtime_adapters_are_supported_only_when_their_producer_emits_metrics`; `::sglang_runtime_adapter_uses_the_same_allowlisted_prometheus_contract`; `::comfy_runtime_adapter_reports_queue_and_explicitly_unsupported_token_metrics`; `control/tests/test_agent_api.py` authenticated telemetry checks; `control/tests/test_fleet_projection.py`; `tests/cluster_profiles/test_controller_cli.py::test_metrics_history_capabilities_and_workloads_use_exact_routes` | vLLM, SGLang, llama.cpp and Comfy fixtures prove parser mapping, queue/KV/cache ratios, histogram p95 and unsupported fields. DS4 and EXL3 are intentionally explicit unavailable rows until truthful adapters exist; they are not claimed as absent from the contract. | The focused Python suite is local evidence only; the Rust runtime fixtures are not executable on this macOS host because of the unrelated `rustix` compile gate. No CI, deployed runtime, or physical throughput/histogram evidence is established. | Add truthful DS4/EXL3 adapters before marking those rows supported; add deployed cross-client runtime/workload and coordinator-versus-rank evidence. |
| A21 | **Partial.** CLI `fleet telemetry`/`fleet metrics` expose current, capabilities, workloads, bounded history filters and unchanged JSON export against the four telemetry operation IDs. Generated OpenAPI/client models are concrete. The web detail has Metrics, Workloads, Services and Events tabs, current/capability/workload polling, provenance/unit/reason rendering and current export; its rich history chart mapping remains pending. | `tests/cluster_profiles/test_controller_cli.py::test_metrics_current_forwards_server_metric_units_and_provenance`; `::test_metrics_history_capabilities_and_workloads_use_exact_routes`; `::test_metrics_export_writes_the_unchanged_server_projection`; `tests/control/test_openapi_clients.py::test_generated_telemetry_contracts_are_concrete_and_versioned`; `control/web/src/api/client.test.ts` telemetry route tests | The focused Python route/query/export checks are local evidence; web client route tests are checked in but no browser visual journey was run for this correction. | No CI, deployed cross-client comparison, complete keyboard/mobile/browser matrix or hardware result is claimed. | Web history still charts legacy scalar keys, does not map rich per-series rollup identity to the detail charts, and its current export omits history. Complete the rich history/current/export parity and the specified 1h/24h/7d/31d keyboard/mobile journeys in the separate web change. |
| A22 | **Partial.** Native collection includes CPU temperature and powercap draw plus GPU measured draw/limit, temperature, clocks and throttle reason flags with source and measurement kind. Controller keeps runtime readiness/error and rank placement separate from host heartbeat and telemetry freshness, so a fresh host cannot overwrite a failed or lost run state. The telemetry producer now preserves unavailable throttle state instead of emitting a false inactive value. | `rust/crates/vonk-agent/tests/telemetry.rs::collects_literal_cpu_memory_disk_network_and_accelerator_metrics`; `::counter_resets_and_missing_sources_remain_unknown`; `::unavailable_throttle_flags_do_not_become_false_active_state`; `::throttle_state_preserves_mixed_device_scopes`; `control/tests/test_fleet_projection.py::test_freshness_boundaries_keep_telemetry_agent_and_inventory_independent`; `control/tests/test_recipe_operations.py::test_run_status_projects_exact_rank_health_without_agent_secrets` | Fixtures prove measured/configured units, missing-source nulls, truthful mixed-device throttle scopes, and stale host/rank projections; they do not drive a 95% busy GPU or actual thermal throttle. | Focused Python evidence is local; Rust execution is blocked by the macOS `rustix` Linux API gate. No deployed, CI or physical healthy-busy/throttle run is established. | Add the 95% busy healthy-runtime versus thermal-throttle/runtime-failure fixture and Spark evidence; keep measured power, configured limit/TDP and estimated power distinct. |
| A23 | **Partial.** Optional runtime collection fails closed to agent-owned run metadata and known loopback/container-local endpoints with bounded timeouts, protocol and payload size; known adapters with unavailable or invalid allowlisted ports produce explicit unsupported reasons, while untrusted metadata is not scanned. ComfyUI queue support is real, while Controller joins only authorized run/job records and the detail Services/Workloads tabs remain read-only. | `rust/crates/vonk-agent/tests/telemetry.rs::comfy_runtime_adapter_reports_queue_and_explicitly_unsupported_token_metrics`; `::namespaced_runtime_adapters_are_supported_only_when_their_producer_emits_metrics`; `::malformed_or_oversized_optional_evidence_is_bounded_to_null_metrics`; `control/tests/test_builtin_harnesses.py::test_comfyui_requires_an_immutable_workflow_from_the_recipe_bundle`; `control/tests/test_metrics.py` allowlist/auth tests; `control/web/src/api/client.test.ts` rich route test | Fixtures prove bounded Comfy queue parsing, llama.cpp mapping, explicit DS4/EXL3 unavailable rows, unsupported token metrics, malformed evidence handling, secret-free capability/route reads and no arbitrary endpoint scan. | No full optional image/service matrix, CI/deployment probe audit or physical optional-runtime evidence is claimed. | Comfy job metadata (title/model/steps/dimensions/progress/ETA), Tailscale/Hermes service health, and truthful DS4/EXL3 adapters remain absent. Malformed runtime contracts are ignored rather than surfaced as a capability row. Add truthful per-capability rows and verify that Fleet reads never benchmark, upgrade, mutate services or probe arbitrary URLs. |

## Packaging and distribution end-to-end journeys

These journeys connect the interface contract to recipe package publication,
Controller persistence, artifact delivery, and the final run/switch action.

| Journey | Status and production binding | Focused test path | Fixture-only evidence | CI, deployment, and hardware boundary | Unresolved gap |
|---|---|---|---|---|---|
| D01 — Convert the recipe library into independent packages | **Local release closure.** The `v1.0.1` schema-2 release at recipe commit `10db3c7a73a18319f81103448ddaeada50334bb2` contains 84 Recipe documents and 92 Model documents; 13 Models have no direct Recipe. | The recorded recipe package consumer evidence is 88 passing fixtures across changed-only, restart, failure, offline and fork behavior. | Package/index closure covers the 84-Recipe release inventory; this does not establish physical runtime acceptance for every Recipe. | Recipe release `v1.0.1` is the current publication authority. No new platform package publication, deployed Controller consumption, or hardware evidence is claimed here. | Add a per-Recipe audit record and retain concrete blockers such as stale/private image pins, missing runtime/source bundles, and unperformed serving or Spark checks. |
| D02 — Persist changed packages atomically and retain the prior generation | **Partial in the platform integration.** Catalog and Library projection cutovers are merged locally at `8930f9fb`; package consumer fixtures remain supporting evidence. | The 88 passing consumer fixtures cover restart, changed-only sync, failure, offline and fork behavior. | Fixture evidence covers package-state behavior, not a deployed Controller upgrade or a physical catalog. | No platform package publication or deployed Controller catalog upgrade is claimed; recipe `v1.0.1` publication remains a separate gate. | Re-run the current-head publication/consumption lane and capture immutable deployed generation retention. |
| D03 — Resolve one exact model/recipe/image artifact set | **Bound.** Model cache manifests, recipe resolution, runtime distribution identity, and run admission bind exact digests and topology. | `control/tests/test_run_switch_operations.py::test_model_cache_manifest_allows_planned_nas_download`; `::test_uncached_run_selects_external_fresh_builder_and_plans_container_phase`; `control/tests/test_catalog_contract.py` exact identity tests | Fixtures reject incomplete/mismatched model and image identities. | No external builder, published OCI registry, or Spark import evidence. | Prove one exact resolved manifest through Controller build/cache and target readiness. |
| D04 — Download/cache once, then distribute to two Sparks | **Partial.** `ModelCacheVerifiedObjectSource`, `DurableArtifactDistributionService`, and per-node child operations are production bindings; the production composition path is covered by six local file-backed cases in `test_production_composite_uncached_cache_then_two_target_distribution`. | `control/tests/test_model_cache.py::test_one_set_with_shared_digest_counts_one_physical_payload`; `control/tests/test_recipe_builds.py::test_distribution_reimports_one_build_digest_for_every_mapped_node`; `control/tests/test_distribution_executor.py::test_production_composite_uncached_cache_then_two_target_distribution` | Disposable targets prove deduplication, destination verification, and per-node progress; the 49-case worker branch is separate evidence. | No authenticated two-Spark deployment, measured transfer, or no-Spark upstream run. External catalog/build and Rust consumer checks remain separate. | Execute the full Controller→two-Spark journey with agent grants and record transfer/import evidence. |
| D05 — Resume interrupted delivery safely | **Bound.** Distribution child checkpoints, restart-safe worker processing, and digest mismatch rejection are wired. | `control/tests/test_run_switch_operations.py::test_artifact_child_checkpoint_and_digest_mismatch_fail_closed`; `control/tests/test_run_switch_operations.py::test_child_distribution_progress_is_typed_and_restart_safe`; `control/tests/test_model_cache.py::test_interrupted_download_checkpoint_resumes_after_service_restart`; `control/tests/test_distribution_executor.py::test_production_composite_uncached_cache_then_two_target_distribution` | Local worker restart fixtures preserve completed bytes and reject modified bytes; six local composition cases and the 49-case worker branch remain separate evidence. | No physical link interruption or deployed fleet interruption. | Run a designated Spark interruption test with durable evidence. |
| D06 — Run/switch performs automatic preparation and reuse | **Partial.** Composite run/switch and profile adapters plan copy/build/prepare/stop/start/final verification under one operation. | `control/tests/test_run_switch_operations.py::test_uncached_build_receipt_reaches_copy_after_restart_without_replay`; `control/tests/test_fleet_profiles.py::test_profile_apply_switches_dual_solo_idle_and_reuses_cached_installation` | Synthetic service graphs prove phase ordering and reuse. | No deployed normal web/CLI action, full profile A→B→A, or physical readiness evidence. | Execute normal Run/Switch without a mandatory preparation ceremony and capture actual operation phases. |
| D07 — Preserve artifacts through cleanup and failure | **Bound.** Cleanup adapters refuse NAS eviction/no-op cleanup, uninstall checks references, and partial operations retain successful work. | `control/tests/test_run_switch_operations.py::test_cleanup_adapter_cannot_evict_nas_or_return_noop`; `control/tests/test_recipe_operations.py::test_partial_multinode_stop_retains_every_active_capacity_reservation`; `control/tests/test_recipe_operations.py::test_model_deletion_requires_explicit_stop_for_every_active_run` | Fixture plans prove scoped cleanup and reference protection. | No live storage lifecycle or deployed failure recovery. | Verify retained NAS/Spark artifacts and blocked cleanup with actual active workloads. |
| D08 — Inspect one operation consistently through web and CLI | **Partial.** Generic Activity providers merge AgentOperation, cache, and profile families; CLI operations commands call the same global routes. | `control/tests/test_operation_api.py::test_global_operation_projection_merges_typed_provider_families`; `::test_profile_operation_provider_is_registered_through_the_global_api`; CLI operation command tests | Two-family fixture list/detail and cursor tests prove typed global projection. | No deployed cross-client creation/inspection or browser operation-detail acceptance. | Complete web↔CLI normalized identity/progress/evidence parity and uncertain-response reconciliation. |

## Evidence boundary and refresh rule

Repository and focused tests establish implementation bindings. Fixture-only
evidence remains explicitly labeled. CI/container checks establish build and
runtime portability only. Controller deployment evidence must name the exact
published source/artifact revisions. Spark hardware evidence must name the
physical nodes, agent/runtime identities, observation time, and operation or
qualification result. None of those boundaries can be inferred from another.

Refresh this ledger after the web, model, cache, telemetry, and distribution
integration pass; preserve unresolved rows until their named journey is run and
its evidence is recorded.
