# Issue 596: recipe-aware Spark runtime preflight

> Historical/as-of status: This is a historical repository/progress/evidence snapshot, valid only as of its stated baseline/date. Do not read it as current deployment state.

Base: `fb2626a00e545138dda6393005490ae73e763014`. Scope: current typed Controller/Spark preflight, exact fenced helper authorization, service-bound offline probes, admission and package fixture. No NAS or physical Spark was changed.

| Criterion | Implementation | Evidence / limit |
|---|---|---|
| Healthy probe under one minute, no production image/model | Fixed static payload (~994 KB), scratch rootfs tar, `--pull=never`, no network; build deadline 40 seconds; helper import/run 10 seconds plus bounded cleanup | OrbStack ARM64, Podman 4.9.3 / crun 1.14.1: actual production agent service unit (only acceptance ExecStart changed), build and non-root run passed in **1,694 ms**; next service execution reused cache in **3 ms**. No production image or model fetched. |
| Effective agent environment | Existing SystemProcessRunner and recipe rootless `systemd-run --user`, crun, cgroupfs, storage options, temporary/runroot layout; serving is separate fixed signed-helper action using normal Docker command runner | Production agent sandbox test retained User/Group, PrivateTmp, namespace restrictions, writable roots, capability bounding set. The generic container has no GPU. Build RUN is container UID 0 with zero effective/permitted/ambient capabilities and no-new-privileges; final runtime UID 65534, read-only root and private tmpfs. |
| Specific host/config findings | Typed capability/status/code, existing secret-free Podman classification plus payload codes for proc, capabilities, no-new-privileges, namespaces and tmp; no raw diagnostics in result | Rust `proc_namespace_and_temporary_storage_failures_have_concrete_findings`; real service test discovered Podman's tmpfs default required explicit `mode=1777`, now fixed. |
| Kernel/agent/runtime/unit/storage invalidation | Fingerprint includes kernel release/boot ID, namespace policy, agent config/subuid/subgid/storage config, runtime/agent/helper file identities and effective systemd unit properties; dynamic fingerprint capability recomputed every claim | `changed_host_or_request_reexecutes_cached_probe_and_refreshes_dynamic_facts`; Controller refuses absent/different fingerprint or stale/request-mismatched result. Dynamic disk, writable roots and fabric are rechecked even on cache reuse. |
| Typed admission and unknown mandatory requirements | Canonical request/result models; persisted fenced attempts validate on read; install consumes target-runtime (source_build=False) requirements; missing mandatory findings block clearly | Python admission integration plus strict request/result validation. Unknown optional capability does not block; unknown custom mandatory capability does. |
| Fabric and disk reserve | Exact fabric topology/speed requirement and disk floor; configured fabric must have a locally bindable address; configured bandwidth is declared capability, not measured fabric throughput | Rust fabric unknown/mismatch and disk-floor tests; Python exact fabric/disk request binding. No NCCL throughput or physical fabric acceptance claimed. |
| Signed helper cannot widen privileges | New `runtime-preflight` action accepts **no arguments**, is authorized only for the exact live preflight job/attempt/fence, executes fixed local import/run with zero caps, no network/device/host bind mounts, then removes its own unique image/container | Python exact grant/stale fence/wrong action tests; Rust fixed-action argument rejection; actual offline Docker scratch non-root/read-only/no-network/zero-cap run printed `vonk-runtime-preflight-ok`. Full signed helper execution uses the normal dispatch path; the manual Docker check is sandbox behavior evidence. |
| Package and wire | Second static `vonk-runtime-probe` binary in existing statically built egress package; required package/binary static-ELF verification; strict Python/Rust transport | Connected Python request -> Rust strict consumer/canonical request digest/result -> Python strict validation passed; binary verifier tests passed. Full dpkg package tests were attempted on macOS (dpkg missing) and OrbStack; chosen API test image lacked `/usr/bin/python3`, so package assembly remains integrated Linux CI verification. |

Focused validation: 83 Python tests across runtime preflight, install admission, host-helper authority, agent jobs and binary verifier. Six Linux Rust preflight tests pass. Linux `cargo check -p vonk-agent --bins` passes. Pinned Ruff 0.16.1 and `git diff --check` pass.

Actual service fixture used `vonk-recovery-systemd:local` in OrbStack with standard D-Bus packages installed only in the disposable fixture. Network was removed before probing. The Rust acceptance driver supplies a fixed test fingerprint and assumes its claim connectivity: this test proves the service/build/run environment, not live Controller connectivity or complete fleet enrollment. It intentionally leaves signed_helper_run unknown; the production executor replaces that finding only after its separately authorized helper result.

Integration boundary: issue 597 owns the durable preflight child scheduling before source/model/image transfer and source builds. The new operation queue, transport, runtime/helper dispatch and install admission are implemented here. Root owns generated OpenAPI/clients/wheels/SBOM and combined publication checks. Install requests must use source_build=False; only the actual builder phase requires rootless build capability. Do not close the issue until the combined scheduling path and generated contracts pass integrated checks.
