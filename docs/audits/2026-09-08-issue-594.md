# Issue 594: reliable operation progress

> Historical/as-of status: This is a historical repository/progress/evidence snapshot, valid only as of its stated baseline/date. Do not read it as current deployment state.

Base platform: `fb2626a00e545138dda6393005490ae73e763014`. This change proves repository behavior, not publication, Controller deployment, or physical Spark acceptance.

## Acceptance matrix

| Criterion | Implementation | Behavioral evidence |
|---|---|---|
| Independent and aggregate bytes, speed, ETA and phase | Canonical `OperationProgress` and member graph add items, smoothed rate, elapsed, observation and advancement times, and advisory activity. `JobProgress.operation` aggregates every current attempt, independently of displayed pagination. Parallel ETA is the slowest known member ETA; any unknown member prevents a made-up aggregate estimate. | `test_parallel_job_byte_aggregate_is_independent_of_operation_page`; `test_parallel_aggregate_eta_uses_slowest_and_unknown_member_stays_unknown` |
| Progress independent of normal telemetry | Existing agent operation heartbeat task remains separate from telemetry. Distribution callbacks publish into a single coalesced watch snapshot at 1 Hz. Generic lease renewal no longer replaces specific phases. Executor phase snapshots cover build, upload, install, extraction, verification and start. | Rust `long_execution_renews_and_persists_its_lease_before_result`; `distribution_acceptance_uses_authenticated_ranges_and_reuses_import_cache` |
| Hashing/extraction/startup remain distinct and indeterminate | Distribution marks copying versus verification before hashes. Runtime helper actions mark extraction/start/stop. Controller projection clears transfer rate and ETA outside transfer phases. | `test_phase_only_lease_renewal_retains_phase_counters_but_verification_clears_eta`; UI `verification clears transfer ETA and keeps known transfer counters indeterminate` |
| Reconnect/retry does not regress completed bytes | Retryable heartbeat transport errors are retried while the current lease remains valid. Distribution retains its byte/item high-water mark across in-process retries and authorized later attempts; replayed earlier objects preserve counters. Final validated success evidence closes coalesced counters. | Rust `transient_heartbeat_failure_does_not_terminate_healthy_execution`; Controller `test_distribution_retry_preserves_durable_progress_and_accepts_object_replay`; `test_successful_distribution_receipt_closes_coalesced_final_counters` |
| Advisory, phase-aware stall status | Fresh phase changes/counters are active. Missing/aged observations are waiting; only an incomplete transfer with no advance for 120 seconds is possibly stalled. This projection does not alter execution, retries, cancellation or leases. Terminal operations do not show active/stalled work or ETA. | `test_stalled_is_advisory_phase_aware_and_stale_rate_is_hidden`; `test_terminal_operation_never_claims_work_is_stalled_or_running` |
| Bounded updates and retained history | One durable snapshot per attempt, without sample history. Byte samples write at most once per second; phase transitions have a ten-write/second ceiling. Transport callbacks use a watch value rather than an event queue. | `test_write_frequency_coalesces_and_retains_one_snapshot`; `test_progress_snapshots_and_phase_changes_have_bounded_write_frequency` |
| API/CLI/UI consistency | Canonical API fields survive strict Python and Rust validation. CLI prints aggregate or direct rate/ETA/elapsed/last advancement. Library and Activity use one compact progress component with indeterminate phases and visibly aged timestamps. | Connected `test_python_progress_crosses_rust_parser_without_losing_measurements`; CLI `test_progress_line_shows_canonical_aggregate_rates_eta_and_freshness`; Playwright `operation-progress.spec.ts` at 1440 and 390 px |

## Verification

- Controller, protocol and projection suites: **100 passed** (`test_operation_progress.py`, `test_operation_contract.py`, `test_agent_jobs.py`, `test_operation_api.py`, `agent_protocol/tests/test_wire_model.py`), with additional focused aggregate/final-result tests passing after their addition.
- OrbStack confirmed `orbstack linux aarch64`, using `rust:1.97.1`, an isolated source mount and task-specific target directory. Agent library **107 passed**, protocol library **19 passed**. Focused final heartbeat **5 passed**, distribution **8 passed**, and typed heartbeat graph **3 passed**.
- Python producer to compiled Linux Rust consumer: **1 passed** via `VONK_PROGRESS_WIRE_PROBE`.
- Web component/Activity tests: **32 passed**. TypeScript and Vite production build pass. Desktop/mobile Playwright: **2 passed**, including axe and overflow checks. One bounded correction/confirmation pass fixed long operation labels and made timestamps visibly aged.
- Ruff **0.16.1**, `cargo fmt`, and `git diff --check` pass.

## Integration and evidence boundaries

Root owns generated OpenAPI/clients and protocol wheel publication. Root connects the existing model-cache DTO into canonical global progress in `ModelCacheOperationProvider._progress`. The `recipe_image_availability_api._progress` boundary now preserves every canonical progress field and has a behavioral regression test. Run-switch/Fleet application progress is a separate current DTO: its propagation is coordinated with issue 597; do not close issue 594 until those combined integrations are verified. Generic operations and the shared Library/Activity views are implemented here.

No NAS/Spark deployment or physical two-Spark transfer was performed. Container acceptance verifies real ranged transfers, partial-file resume, immutable identity checks and local image-cache handoff; it does not prove NVIDIA model loading or fabric throughput. ETA is a measured transfer estimate, not an estimate of later build, verification, extraction or startup work.
