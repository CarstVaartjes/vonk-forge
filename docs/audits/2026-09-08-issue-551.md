# Issue 551 — captured-source package rollback

> Historical/as-of status: This is a historical repository/progress/evidence snapshot, valid only as of its stated baseline/date. Do not read it as current deployment state.

Repository implementation and disposable ARM64 acceptance; no NAS, enrolled
Spark, publication, or physical GPU mutation was performed.

## Acceptance matrix

| Criterion | Current implementation | Verification and boundary |
| --- | --- | --- |
| Current signed self-update, sanitized systemd/ARM64 | Required nested `PackageRollbackAuthority`; both source and candidate signatures verified and copied into root custody before `dpkg`; only current protocol accepted | Actual OrbStack `orbstack`, Linux `aarch64`, systemd PID 1, real signed DEBs/current helper/current preinst. Two current-format fixture generations; no invented oldest supported historic release |
| Missing prerequisites preserve healthy source | Fixed executable/ownership checks, configured package/source hashes, enabled watchdog, metadata and anti-downgrade preflight before old `prerm` | Removing `logger` and providing wrong source hash both rejected with the same source MainPID retained; an arbitrary older signed candidate is rejected with the same candidate MainPID retained |
| Failed activation restores source | Root-retained helper watchdog; deadline or installation failure restores only the captured DEB; configured package and actual `/proc/PID/exe` hash verified | No acknowledgement/wrong nonce, failed postinst, and interrupted source configuration all reached `rolled_back`; source version/package/agent hashes and process identity matched |
| Rollback authority remains narrow | Source digest/signature/version/agent/helper hashes, node, nonce and deadline; root-private transaction; fixed watchdog cgroup proven by systemd; fixed preinst validator; no arbitrary operation/command/path or downgrade | Signed helper authority tests reject unsafe paths/commands; wrong acknowledgement nonce rejects; direct prior DEB plus copied nonce outside watchdog cannot pass current downgrade guard |
| One-at-a-time canary waits for activation | Fixed signed `confirm-package-activation` binds node, candidate package and original nonce; only acknowledged receipt cancels host watchdog | Exact acknowledgement kept candidate beyond deadline. The integrated Controller and agent stage the published source package, exchange the typed activation grant after candidate inventory/self-test, and require the matching acknowledged receipt before advancing. Rollback receipts stop the canary. Connected Controller tests cover the gate; physical reconnect remains a separate deployment check. |
| Evidence records source/candidate/node/outcome/time | Canonical strict Python/Rust `PackageActivationReceipt`, root-owned readable current schema-2 receipt | Real receipts recorded package and binary digests, versions, node, nonce, timestamps, `acknowledged` / `rolled_back` and bounded outcomes. Python rejects coercions, old schema, unbounded output and reversed timestamps |
| Regression gate | Existing native ARM64 recovery workflow builds helper + acceptance probe and runs the new matrix | `.github/workflows/agent-upgrade-recovery.yml` adds actual package acceptance after existing full production capsule lanes; no test-only production bypass |

## Verification

- `PYTHONPATH=agent_protocol/src:control/src:src .../control/.venv/bin/python -m pytest -q agent_protocol/tests/test_package_upgrade.py agent_protocol/tests/test_package_helper_authority.py`: **21 passed**.
- Real ARM64 `cargo test --locked -p vonk-agent-helper --tests`: **63 passed**.
- Actual acceptance: `cargo build --locked -p vonk-agent-helper --bin vonk-agent-helper --example package_rollback_probe`, then `python3 tests/nodes/test_agent_package_rollback_systemd.py --disposable-systemd --helper .../debug/vonk-agent-helper --probe .../debug/examples/package_rollback_probe`: **all five matrix cases passed**, including separate exact-ack, wrong-nonce, arbitrary-downgrade and interrupted-rollback assertions.
- Local evidence retained at `/private/tmp/vonk-issues-20260908/issue-551-arm64-rollback-evidence.json`.
- Pinned Ruff 0.16.1, Rust formatting, shell syntax, and `git diff --check`: passed.

The new matrix uses a small explicit service executable and postinst to inject
faults; artifact signature checking, helper custody/execution, the current
production preinst downgrade guard, dpkg, watchdog unit, restart and process
identity proof are real. The adjacent capsule/repair shell lanes exercise the
complete production maintainer scripts and now stage the required current
`vonk-runtime-probe` binary from #596. They need the whole integrated #596
branch; their new binary-staging lines were syntax checked here, not represented
as a separate local full-lane success. Live Controller reconnect and physical
Spark acceptance remain distinct evidence boundaries.
