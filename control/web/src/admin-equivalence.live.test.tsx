/**
 * The pre-v1 profile/reconciliation browser equivalence flow was retired with
 * the profile authority. Current Catalog, Library, package, deployment, and
 * run flows are covered by their route-specific tests.
 */
it.skip("legacy profile/reconciliation browser equivalence flow is retired", () => {});
